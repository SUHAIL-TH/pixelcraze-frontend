import { ProfessionalsearchPipe } from './professionalsearch.pipe';

describe('ProfessionalsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfessionalsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
