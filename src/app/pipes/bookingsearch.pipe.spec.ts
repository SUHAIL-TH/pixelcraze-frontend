import { BookingsearchPipe } from './bookingsearch.pipe';

describe('BookingsearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingsearchPipe();
    expect(pipe).toBeTruthy();
  });
});
