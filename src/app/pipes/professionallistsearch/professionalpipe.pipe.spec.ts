import { ProfessionalpipePipe } from './professionalpipe.pipe';

describe('ProfessionalpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProfessionalpipePipe();
    expect(pipe).toBeTruthy();
  });
});
